server:
  port: 9000
spring:
  datasource:
    url: jdbc:mysql://localhost:3308/loansdb
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none
  sql:
    init:
      mode: always
